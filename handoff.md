# Handoff: QS tile notice (subtitle pill vs notification) + Context7 MCP

## Goal
Make Quick Settings tile show a transient **tile notice** (subtitle pill at bottom after tap, like VPN tiles "Connecting…") when switching refresh rate, **not** a persistent shade `Notification` nor a `Toast`. Current scope: fix `RefreshRateTileService` to show `Connecting…` when Shizuku not bound and `Switching to X Hz…` → `X Hz` when cycling, on MIUI (Xiaomi, ControlCenter) and stock Android. Original asks: "when i change hz from tile it dont show a notice… like vpn tile shows connecting (NOT A TOAST)"; then "fix that and if i click tile and app wasnt connected, show connecting in notice not toast, lint, build debug and install via adb"; reported "didnt work, either connecting or when i change hz"; then "its doing actual notification not the notice"; then request to add Context7 MCP at https://mcp.context7.com/mcp and verify, then "look up docs and find out how to actually do this".

## Current state
- Branch `beta` @ `72ac8de`, `main` @ `f6e1ef8` (0.0.43). `beta` ahead by 4 commits, working tree **dirty** with uncommitted tile fix (see below). `git status` clean except modified + untracked.
- Latest CI green: `CI beta 2026-08-29 03:29 2m18s`, `Release Beta v0.0.44-beta.1 2026-08-29T03:35:11Z`.
- Tile fix iteration 1 (subtitle only) built `app/build/outputs/apk_from_bundle/debug/app-debug-universal.apk` (17M) and `adb -s 192.168.1.10:5555 install -r` succeeded (`akihz.anlaki.dev.debug v0.0-debug`). User reported "didnt work".
- Iteration 2 added `TileFeedbackNotification` posting actual `Notification` (ID 1002, channel `akihz_tile_feedback` HIGH, timeout 2500ms). User reported "its doing actual notification not the notice".
- Iteration 3 (current, **not yet verified with user**) removed `Notification` posting from tile path, kept only `Tile.subtitle` + `updateTile()` pill, with `onStartListening` cancelling leftover ID 1002. Build `lint` passed (1m46s), `packageDebugUniversalApk` passed (34s), installed to `192.168.1.10:5555` debug, cancelled old notification, no active `akihz` notifications per `dumpsys notification`. No user test yet for this iteration.
- Context7 MCP added at `opencode.json:1` (`mcp.context7.type=remote url=https://mcp.context7.com/mcp enabled:true`), `opencode mcp list` shows `✓ context7 connected @ https://mcp.context7.com/mcp` (1 server). Tested via `curl POST initialize` (requires `Accept: application/json, text/event-stream`) returned `serverInfo Context7 v4.0.4`, and `context7_resolve-library-id` for `Next.js` → `/vercel/next.js` succeeded.
- Docs lookup attempted via `context7_query-docs` on `/websites/developer_android` for `TileService` (returned Wear OS tile docs, not QS), then `websearch` for `Android TileService Quick Settings tile subtitle updateTile notice after click 2025` gave excerpts from `developer.android.com/develop/ui/views/quicksettings-tiles` and AOSP `Tile.java`/`qs-tiles.md`. Real verification still pending: need to confirm MIUI pill behavior vs stock Android subtitle pill.

## Files involved
- `app/src/main/kotlin/akihz/anlaki/dev/presentation/RefreshRateTileService.kt:1-234` — Quick Settings `TileService`. Core of fix. Now has `updateTileConnecting():194` (`subtitle="Connecting..."`), `updateTileSwitching():203` (`subtitle="Switching to ${rate}Hz..."`), `updateTileWithRate():184` (`subtitle="X Hz"`), `onClick():72` logs and branches, `bindAndCycleRate():102` sets `isConnecting` + `updateTileConnecting()` + `Timber`, `cycleRate():127` sets `isSwitching` + `updateTileSwitching()` + `Timber`, cancels notification in `onStartListening():36`. Removed `Intent` import, added `TileFeedbackNotification` + `Timber`.
- `app/src/main/kotlin/akihz/anlaki/dev/utils/TileFeedbackNotification.kt:1-97` — **Untracked new file**. Helper that posts short-lived status notices via `NotificationChannel` `akihz_tile_feedback` (HIGH) with `setTimeoutAfter(2500)` and fallback to `Toast` when `POST_NOTIFICATIONS` denied or channel `IMPORTANCE_NONE`. Also provides `cancel():42`. Currently **not used** by tile path (after iteration 3) except `cancel()`; used by `DebugCliReceiver.testTile` for manual testing. Kept for future but not triggered on real tile tap.
- `app/src/main/kotlin/akihz/anlaki/dev/DebugCliReceiver.kt:69-212` — Added `test_tile` command (`handleCommand:81`) that posts `showConnecting` → `showSwitching` → `showSwitched` with 400ms sleeps and returns channel importance + `areNotificationsEnabled`. Useful for `adb -s 192.168.1.10:5555 shell am broadcast -n akihz.anlaki.dev.debug/akihz.anlaki.dev.DebugCliReceiver -a akihz.anlaki.dev.DEBUG_CLI --es cmd test_tile --es arg 90` and reading via `run-as ... cat /data/data/.../files/debug_cli_output.txt`.
- `app/src/main/AndroidManifest.xml:67-76` — Declares `RefreshRateTileService` (`android.permission.BIND_QUICK_SETTINGS_TILE`, `active_tile` not declared → non-active mode). No change this session but relevant: tile is non-active, bound only when visible; `qsTile` null after `onStopListening`.
- `opencode.json:1` — **Untracked new file**. Project MCP config with `$schema` and `mcp.context7`. Verified `opencode mcp list` shows connected.
- `~/.config/opencode/opencode.jsonc:1` — Global config, currently only `{"$schema":...}`; unchanged.
- `scripts/debug-cli.sh:57-68` — Helper for `list-crashes` etc., uses `am broadcast -n $PKG/akihz.anlaki.dev.DebugCliReceiver`. Not modified but used for verification (`./scripts/debug-cli.sh list-crashes` works).

## What's changed this session
- Uncommitted diff vs HEAD (`git diff --stat`):
  ```
  .../DebugCliReceiver.kt          | 13 ++++++++
  .../presentation/RefreshRateTileService.kt | 38 +++++++++++++++++++---
  2 files changed, 47 insertions(+), 4 deletions(-)
  ```
  plus untracked `TileFeedbackNotification.kt` and `opencode.json`.
- Detailed diff `RefreshRateTileService.kt` vs `dc8a302` (previous): removed `Intent` import, added `TileFeedbackNotification` + `Timber`, `onStartListening` now cancels ID 1002, `onClick` adds `Timber.d` log, error branches no longer call `TileFeedbackNotification.showError`, `bindAndCycleRate` now calls `updateTileConnecting()` then `Timber` (no Toast/Notification), `onConnected` now sets `isConnecting=false` **before** `cycleRate()` (previously after), `onFailed` adds `Timber.w`, `cycleRate` now calls `updateTileSwitching()` not `updateTileWithRate`, adds `Timber`, `onSuccess` now calls `updateTileWithRate()` + `Timber` (no `showSwitched` notification), `onError` adds `Timber`, added `updateTileConnecting()` and `updateTileSwitching()` methods (previously only `updateTileWithRate`). Final file 234 lines (slightly over 200, acceptable per `AGENTS.md:1`).
- `DebugCliReceiver.kt` added `test_tile` handling and `testTile()` function posting via `TileFeedbackNotification` (for manual shade notification test, not for real tile).
- Built debug universal APK twice: `17M` at `app/build/outputs/apk_from_bundle/debug/app-debug-universal.apk`, `lint` passed both times, `packageDebugUniversalApk` passed.
- Installed to device `192.168.1.10:5555` (adb `device`), both packages `akihz.anlaki.dev` (release) and `akihz.anlaki.dev.debug` (debug) present; debug `POST_NOTIFICATIONS` granted via `pm grant`, `dumpsys notification` shows channel `akihz_tile_feedback` importance 4 (HIGH) and no active records after iteration 3.

## Constraints and things to avoid
- `AGENTS.md:6-8` — max 200 lines per file (slightly over OK, don't split unless nonsensical), package structure `data/ domain/ presentation/ di/ utils/`, single responsibility, KDoc for public fns.
- `AGENTS.md:15-16` — don't run local Gradle builds/lint/tests/APK in Android/Termux env; use GitHub Actions CI. Here environment is Linux (`platform:linux`), so local `./gradlew lint` + `packageDebugUniversalApk` allowed per user explicit request; otherwise prefer CI.
- `AGENTS.md:21-28` — releases manual-only, channel-locked (`release-beta.yml` only on `beta`, `release-main.yml` only on `main`), CI runs lint+unit tests separately.
- `AGENTS.md:31-33` — changelog before release, `release-metadata.json` notes.
- Tile UX constraint: **do not** use persistent shade `Notification` for tile feedback; user explicitly wants transient tile subtitle pill (like VPN tile `Connecting…` at bottom after tap, not in shade), which is `Tile.subtitle` + `updateTile()` shown by SystemUI as pill after `onClick`. `Toast` also rejected by user ("NOT A TOAST"). Keep `Toast` only for hard errors if unavoidable, but prefer subtitle.
- Don't assume `qsTile` non-null after QS collapses (`onStopListening` → `qsTile` null). Async `setRate` finishing after collapse loses tile update; subtitle pill only works if `updateTile()` called synchronously in `onClick` before collapse. That's why `Connecting…`/`Switching…` must be set **immediately** in `onClick`/`cycleRate` before async work.
- MIUI (Xiaomi) Control Center heavily customizes QS; subtitle pill may be suppressed vs stock Android. VPN tile still shows pill on this device, so assume MIUI does show pill for subtitle changes—verify on this `sdk=35 release=15` device via real tap + logcat.

## What's been tried and failed
- **Subtitle-only (iteration 1)**: Changed `onClick` to remove `Toast("Connecting...")`, added `updateTileConnecting()` + `updateTileSwitching()` and optimistic `updateTileWithRate` → `updateTileSwitching`. Built + installed debug. **User result: "didnt work, either connecting or when i change hz"**. Likely reason: subtitle pill not seen because QS collapses immediately after tap, and user didn't reopen QS; also async `updateTileWithRate` after `setRate` lost when `qsTile` null, and no pill shown on MIUI if not requested correctly. Also had bug `isConnecting=false` after `cycleRate()` (now fixed to before).
- **Notification via TileFeedbackNotification (iteration 2)**: Added `TileFeedbackNotification` posting `Notification` ID 1002 with `HIGH` + `timeoutAfter 2500` for `Connecting…`/`Switching…`/`Switched…`. Built + installed, `pm grant POST_NOTIFICATIONS` to debug (global importance `NONE` → `DEFAULT`). **User result: "its doing actual notification not the notice when i click on tile"**. Reason: posted to shade (persistent) instead of transient pill; SystemUI pill is `Tile.subtitle`, not `Notification`. Also `test_tile` broadcast via `DebugCliReceiver` posted shade notification, confirming channel works but wrong UX per user.

## Other learnings
- Device is Xiaomi MIUI: logs show `MIUIInput`, `MiuiTcpSocketTracker`, `ControlCenterHeaderExpandController`, `SmartPower`; SDK 35. Both tiles `custom(akihz.anlaki.dev/...)` and `custom(akihz.anlaki.dev.debug/...)` in `sysui_qs_tiles`. `adb shell cmd statusbar click-tile custom(...)` logs `command click tile: custom(...)` but does **not** trigger `TileService.onClick` (requires real touch or `TileService.requestListeningState`); `adb am broadcast` to `DebugCliReceiver` works but only after process started (`WarmPower idle->background` after ~36s). Use `scripts/debug-cli.sh list-crashes` pattern (explicit `-n $PKG/...DebugCliReceiver`) to verify delivery via `debug_cli_output.txt` + `logcat | grep DebugCli`.
- Notification channel `akihz_tile_feedback` created HIGH, but debug global `importance=NONE` blocks shade notifications until `pm grant POST_NOTIFICATIONS`; foreground `KeepAliveService` channel `akihz_keep_alive` LOW still works (foreground exempt) but `numPostedByApp=0` for debug suggests `KeepAliveService` not running (likely `keepAliveEnabled` true by default but service not started until `MainActivity` resume).
- Context7 MCP remote at `https://mcp.context7.com/mcp` works without auth but requires `Accept: application/json, text/event-stream` header; plain `curl -i` returns `405 Method not allowed` + `WWW-Authenticate: Bearer resource_metadata=...`; correct `initialize` POST with `Accept` returns `serverInfo Context7 v4.0.4` via SSE `event: message`. `opencode mcp list` confirms `context7` connected after adding `opencode.json`.
- `developer.android.com` Quick Settings Tiles docs confirm: use `TileService`, manage `Tile` fields `setLabel/setSubtitle/setState/setIcon` + `updateTile()`; `onClick` should update tile and call `updateTile()`; `setSubtitle` shows secondary label (available Android 13+ API 33); active vs non-active mode (manifest `META_DATA_ACTIVE_TILE`). SystemUI `qs-tiles.md` notes tile bound when visible, unbound 30s after `onStopListening`; active tile needs `requestListeningState`.

## Next steps
- **Verify iteration 3 on device** (highest priority): With current build (subtitle-only + cancel, no shade notification), have user pull down QS, tap debug tile (ensure it's the debug tile; both release+debug share label `akiHz`—consider renaming debug label to `akiHz debug` temporarily or remove release tile), observe bottom pill after collapse. Check `adb logcat -d | grep -i "Tile onClick|Tile cycleRate|Tile bindAndCycle"` to confirm code path hit. If still not visible on MIUI, try `Tile.STATE_INACTIVE` for connecting/switching or add `requestListeningState` call to force SystemUI refresh, or inspect VPN tile's real implementation via `dumpsys` on `ch.protonvpn.android:quick_tile_service`.
- **Look up docs properly** (user explicitly asked): Use Context7 `/websites/developer_android` query `TileService setSubtitle updateTile` + `websearch` result `developer.android.com/develop/ui/views/quicksettings-tiles` and sample `android/platform-samples/samples/user-interface/quicksettings`. Confirm: `Tile.subtitle` + `updateTile()` is the correct way to show notice, and that SystemUI shows it as transient pill without needing `Notification` or `Toast`. Document finding in code comment/KDoc. If Context7 docs insufficient, fall back to `webfetch` of `developer.android.com` article (currently timed out, try via `curl` text extraction) and AOSP `Tile.java` source.
- **Decide on `TileFeedbackNotification` fate**: It's currently untracked and not used by tile; either delete file + `test_tile` debug command + `TileFeedbackNotification` import, or keep for manual testing but ensure not called on real tile tap. If deleting, remove `opencode.json` if Context7 MCP should be global not project.
- **Commit or revert**: If pill verified, `git add app/src/main/kotlin/akihz/anlaki/dev/presentation/RefreshRateTileService.kt app/src/main/kotlin/akihz/anlaki/dev/utils/TileFeedbackNotification.kt app/src/main/kotlin/akihz/anlaki/dev/DebugCliReceiver.kt opencode.json` (but consider not committing `opencode.json` or `TileFeedbackNotification` if they were only debug). Run `./gradlew lint` + unit tests (via CI) before pushing to `beta`.

## How to verify current state
- `git status` (should show modified `RefreshRateTileService.kt`, `DebugCliReceiver.kt`, untracked `TileFeedbackNotification.kt`, `opencode.json`)
- `git diff HEAD --stat` (expect 2 modified files + 2 untracked)
- `opencode mcp list` → `✓ context7 connected @ https://mcp.context7.com/mcp`
- `curl -s -H "Accept: application/json, text/event-stream" -H "Content-Type: application/json" -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"1.0"}}}' https://mcp.context7.com/mcp | head`
- `./gradlew lint --parallel --build-cache --no-daemon` (expect `BUILD SUCCESSFUL` + `lint-results-debug.html`)
- `./gradlew :app:packageDebugUniversalApk` (expect `BUILD SUCCESSFUL` + `app/build/outputs/apk_from_bundle/debug/app-debug-universal.apk` 17M)
- `adb -s 192.168.1.10:5555 shell dumpsys notification | grep -E "NotificationRecord.*akihz"` (expect none or only shell_cmd, not `akihz_tile_feedback` active after iteration 3)
- `adb -s 192.168.1.10:5555 shell logcat -c; <tap tile>; adb -s 192.168.1.10:5555 shell logcat -d | grep -i "Tile"` (expect `Tile onClick ...` + `Tile cycleRate: switching to ...`)
- `./scripts/debug-cli.sh list-crashes` (expect `No crashes stored` + `debug_cli_output.txt` created)
